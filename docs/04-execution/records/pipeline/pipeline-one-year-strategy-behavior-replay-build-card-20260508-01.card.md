# Pipeline One-Year Strategy Behavior Replay Build Card

日期：2026-05-08

状态：`blocked`

## 1. 目标

在已冻结边界内，实际跑完 `2024` 年的策略行为回放，给出信号、持仓、组合计划、订单意图、
执行计划和拒单分布的第一份正式观察证据。

## 2. 观察边界

| 项 | 值 |
|---|---|
| module_scope | `year_replay` |
| target_year | `2024` |
| release expectation | `完整自然年覆盖才可 passed` |
| not claimed | `real fill-backed pnl / cash ledger / production release / v1 complete` |

## 3. 当前结果

本卡已执行，但因 `2024` 覆盖从 `2024-01-08` 才开始，未满足完整自然年要求，当前结论为 `blocked`。
