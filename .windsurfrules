# Windsurf Cascade Rules — Asteria Project

## 工具路由优先级

1. **本地代码语义检索** → 优先使用 MCP `codebase-retrieval`（Augment Context Engine，实时索引工作目录）；不可用时退回内置 `code_search`（Fast Context）
2. **外部库官方文档** → 优先使用 MCP `context7`
3. **指定 URL 内容抓取** → 优先使用 MCP `fetch`
4. **复杂推理、迁移规划、排错** → 必要时使用 MCP `sequential-thinking`
5. **不要先用 grep/find** 代替语义检索
6. **结论优先** → 先给结论和关键证据，再展开细节

## 项目基本约束

- Python 环境：`H:\Asteria\.venv\Scripts\python.exe`，基础 provider `D:\miniconda\py310`
- 正式数据库：`H:\Asteria-data`（不得写临时产物）
- 临时产物：`H:\Asteria-temp`（pytest cache、working DB、ruff/mypy cache）
- 权威资产目录：`H:\Asteria-Validated`（只读输入，不作 scratch）

## 编辑硬规则

- 修改任何模块前，必须先用 `code_search` 理解当前实现，再给方案
- 一次 turn 只允许编辑一个主线模块
- 不得在没有权威设计文档的情况下向主线迁入 legacy code
- 下游模块不得重定义上游模块语义
- `data` 层是 foundation infrastructure，不是策略层
- Alpha、Signal、Portfolio、Trade、System 不得写回 MALF
- Python 文件 ≤ 500 行；脚本 wrapper ≤ 240 行；Markdown 设计文档 ≤ 1200 行

## 治理检查（结构性变更前必跑）

```
python scripts\governance\check_project_governance.py
ruff check . --cache-dir H:\Asteria-temp\ruff-cache
ruff format --check . --cache-dir H:\Asteria-temp\ruff-cache
mypy src --cache-dir H:\Asteria-temp\mypy-cache
```

## 当前门禁状态

- **当前 active module**：`pipeline`
- **当前允许下一卡**：`pipeline_full_chain_bounded_proof_build_card`（run_id: `pipeline-full-chain-bounded-proof-build-card-20260508-01`，prepared / not executed）
- 已通过：Trade bounded proof（`trade-bounded-proof-build-card-20260507-01`）、System Readout freeze review、System Readout bounded proof、Pipeline freeze review、Pipeline build/runtime authorization scope freeze、Pipeline single-module orchestration build、Pipeline full-chain dry-run authorization scope freeze、Pipeline full-chain dry-run、Pipeline full-chain bounded proof authorization scope freeze
- `trade.duckdb` day bounded surface 已存在；`system.duckdb` day bounded surface 已存在；`pipeline.duckdb` 已存在（含 system_readout 单模块 orchestration run + full_chain_day dry-run run）
- **已授权**：执行 `pipeline-full-chain-bounded-proof-build-card-20260508-01`，在 released day bounded surfaces 上跑全链路 bounded proof runtime
- **未授权**：Trade full build、Position/Portfolio Plan full build、System full build、任何模块 full build、full-chain bounded proof passed 扩权

## 治理权威源（每次施工前必须核对）

- `governance/module_gate_registry.toml` — 模块门禁与 allow_build 权威
- `governance/database_topology_registry.toml` — DB 拓扑与 released/blocked DB 权威
- `governance/historical_ledger_registry.toml` — 历史账本协议
- `governance/module_api_contracts/<module>.toml` — 模块 API 契约
- `docs/03-refactor/00-module-gate-ledger-v1.md` — 门禁账本
- `docs/04-execution/00-conclusion-index-v1.md` — 结论索引

## 模块主线顺序

MALF ✅ → Alpha ✅ → Signal ✅ → Position ✅ → Portfolio Plan ✅ → Trade ✅ → System Readout ✅ → **Pipeline**（当前，full-chain bounded proof build card prepared / not executed）
